#if !defined (PARAMS_H)
#define PARAMS_H
// Bartosz Milewski (c) 2000
#include <windows.h>

const UINT MSG_MEMCLEAR = WM_USER;

enum
{
	VALUE_CHARS = 10, // size in chars of the value field
};

enum
{
	WIN_WIDTH = 400,
	WIN_HEIGHT = 400
};

#endif
